import React from 'react';
import './SearchError.css';

const SearchError = () => {
    return (
        <p className="search-error">
            Ничего не найдено.
        </p>
    )
}

export default SearchError;